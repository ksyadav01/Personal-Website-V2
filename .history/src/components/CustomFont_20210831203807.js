import * as React from "react"

const CustomFont = (props) => {
    

    const font = {
        
    }
    return(
        <div style={font}>
            {props.text}
        </div>
    )
  };
  
  export default CustomFont;